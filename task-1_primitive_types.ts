// Задача 1:
// Создай тип User, который включает:

// name (строка)

// age (число)

// isAdmin (логическое значение)

// Затем создай переменную user с этим типом и заполни её произвольными данными.

type User = {
    name: string;
    age: number;
    isAdmin: boolean;
}